export type ModuleCalculationStatus = 'idle' | 'calculating' | 'success' | 'error';

export type ModuleBannerVariant = 'success' | 'warning' | 'error' | 'info';

export interface MethodTemplateLabels {
  calculatorTab: string;
  theoryTab: string;
  inputSectionTitle: string;
}
